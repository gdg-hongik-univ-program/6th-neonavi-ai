# 이 파일을 `.env` 로 복사한 뒤 실제 키를 채운다. (.env 는 git 에 올라가지 않음)
# 키는 안중현에게 요청. 자세한 설치 순서는 SETUP.md 참고.

# 카카오 REST API 키 — 길찾기·장소검색(서버에서 호출)
# Kakao Developers > 내 애플리케이션 > 플랫폼 키 > REST API 키
KAKAO_REST_API_KEY=
